export instance_db_id=$instance.id;
#if ($instance.attributes)
#foreach ($attribute in $instance.attributes)
#if ($attribute.autoExportToScript == true and $attribute.typeClassName != "byte[]")
export $attribute.name="$(cat << EOF
attribute.value
EOF
)";
#end  
#end
#end 
