container_db_id=$container.id
container_name=$container.name
container_description=$container.description

#foreach ($containerVar in $container.getContainerVars())
$containerVar.attributeName=$containerVar.attributeValue
#end
